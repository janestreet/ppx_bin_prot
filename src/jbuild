(library
 ((name ppx_bin_prot)
  (public_name ppx_bin_prot)
  (public_release ((kind ppx_type_conv_plugin)))
  (wrapped false)
  (libraries (ocamlcommon ppx_core ppx_type_conv bin_shape_expand))
  (preprocess ((metaquot All)))))
